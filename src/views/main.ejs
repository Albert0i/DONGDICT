<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Main Page</title>
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-md mx-auto">
        <h1 class="text-2xl font-bold mb-4">Results for "<%= searchTerm %>"</h1>
        <ul>
            <% matchedWords.forEach(word => { %>
                <li>
                    <a href="/detail?word=<%= word %>" class="text-blue-500 hover:underline"><%= word %></a>
                </li>
            <% }) %>
        </ul>
        <% if (matchedWords.length === 0) { %>
            <p>No matching words found.</p>
        <% } %>
        <a href="/" class="mt-4 inline-block bg-blue-500 text-white px-4 py-2 rounded">Back to Search</a>
    </div>
</body>
</html>