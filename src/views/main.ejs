<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Dong Dict - Search Result</title>
    <link rel="icon" href="favicon.ico" type="image/x-icon">
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-md mx-auto">        
        
        <% if (matchedWords.length > 0) { %>
            <h1 class="text-2xl font-bold mb-4"><%=matchedWords.length %> Result<%=matchedWords.length > 1 ? "s":"" %> for "<%= searchTerm %>"</h1>

            <ol class="list-decimal list-inside p-4 bg-white rounded shadow-md">
                <% matchedWords.forEach(word => { %>
                    <li class="text-lg text-gray-800 mb-2 hover:text-blue-500">
                        <a href="/detail?word=<%= word %>" class="text-blue-500 hover:underline"><%= word %></a>
                    </li>
                <% }) %>
            </ol>
        <% } %>
        
        <% if (matchedWords.length === 0) { %>
            <h1 class="text-2xl font-bold mb-4">I'm so sorry!</h1>
            <p>No matching words found.</p>
        <% } %>

        <a href="/" class="mt-4 inline-block bg-blue-500 text-white px-4 py-2 rounded">Back to Search</a>
    </div>
</body>
</html>